# Experiment / RL tooling (the Qt game itself has no Python deps).
# Install: /c/Python313/python -m pip install -r requirements-experiments.txt
torch>=2.0
gymnasium>=1.2
numpy>=1.24
matplotlib>=3.7
networkx>=3.0
pytest>=7.0
